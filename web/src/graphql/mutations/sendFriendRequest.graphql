mutation SendFriendRequest($otherId: Int!) {
    sendFriendRequest(otherId: $otherId) {
        friend {
            ...FriendFragment
        }
        errors {
            field
            message
        }
    }
}
