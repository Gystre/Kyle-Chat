mutation CreateDirectMessage($otherId: Int!) {
    createDirectMessage(otherId: $otherId) {
        group {
            ...GroupFragment
        }

        errors {
            ...RegularError
        }
    }
}
